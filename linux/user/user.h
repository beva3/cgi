#ifndef __USER__
#define __USER__

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char*affect(char*dest,const char*scr);
void echotab(char**t);
int str2_len(char**t);
char**tab_user();

#endif